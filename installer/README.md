# Installer moved

Installer is now standalone at `../EnginotechC-Installer/` (sibling folder).

See `../EnginotechC-Installer/build-installer.ps1` to build `EnginotechC-Setup-v*.exe` like `python-3.12.0-amd64.exe`.

It auto-installs all standard libraries via package manager on install.

